"use client";
import { useState } from "react"; import PageIntro from "@/components/shared/PageIntro";
export default function SettingsPage(){const [app,setApp]=useState("English");const [plant,setPlant]=useState("Telugu");return <main className="page-wrap"><PageIntro eyebrow="Personalization" title="Settings" description="Control interface language, plant-name language, AI response style, notifications, privacy, and device preferences."/><div className="mt-8 grid gap-6 lg:grid-cols-2"><section className="dashboard-panel"><h2 className="text-xl font-semibold">Language & translation</h2><label className="mt-5 block text-sm">App language<select value={app} onChange={e=>setApp(e.target.value)} className="mt-2 w-full rounded-2xl border border-[var(--border-color)] bg-[var(--surface-secondary)] p-3"><option>English</option><option>Telugu</option><option>Hindi</option></select></label><label className="mt-4 block text-sm">Plant-name language<select value={plant} onChange={e=>setPlant(e.target.value)} className="mt-2 w-full rounded-2xl border border-[var(--border-color)] bg-[var(--surface-secondary)] p-3"><option>Telugu</option><option>English</option><option>Hindi</option></select></label><div className="mt-5 space-y-3">{["Show English name","Show scientific name","Translate selected text","Read translations aloud"].map(x=><label key={x} className="flex gap-3"><input type="checkbox" defaultChecked/>{x}</label>)}</div></section><section className="dashboard-panel"><h2 className="text-xl font-semibold">Privacy & services</h2><div className="mt-5 space-y-3">{["Save scans to Plant Memory","Use location for weather-aware advice","Allow camera and microphone when requested","Receive care reminders"].map(x=><label key={x} className="flex gap-3"><input type="checkbox" defaultChecked/>{x}</label>)}</div><p className="mt-6 text-sm text-[var(--text-secondary)]">API keys remain server-side through Vercel environment variables and must never be committed to GitHub.</p></section></div></main>}
