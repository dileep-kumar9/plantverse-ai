"use client";
import { useState } from "react"; import PageIntro from "@/components/shared/PageIntro"; import { readStore,writeStore } from "@/lib/local-store";
const products=[{id:1,name:"Digital Soil Moisture Meter",price:899,icon:"💧",tag:"App-friendly"},{id:2,name:"Bluetooth Soil Sensor",price:1499,icon:"📡",tag:"Partial support"},{id:3,name:"Organic Compost 5 kg",price:399,icon:"🪴",tag:"Soil improvement"},{id:4,name:"Neem Garden Spray",price:299,icon:"🌿",tag:"Low-risk option"}];
export default function MarketplacePage(){const [cart,setCart]=useState<number[]>(()=>[]); function add(id:number){const next=[...cart,id];setCart(next);writeStore("plantverse-cart",next)} return <main className="page-wrap"><PageIntro eyebrow="Curated supplies" title="Marketplace" description="Compare compatible meters, soil improvements, and treatments. Real checkout adapters are separated from the catalogue." action={<div className="health-pill">Cart {cart.length}</div>}/><div className="mt-8 grid gap-5 sm:grid-cols-2 xl:grid-cols-4">{products.map(p=><article key={p.id} className="feature-card"><div className="text-5xl">{p.icon}</div><span className="mt-5 inline-block rounded-full bg-[var(--brand-soft)] px-3 py-1 text-xs text-[var(--brand-primary)]">{p.tag}</span><h2 className="mt-4 text-lg font-semibold">{p.name}</h2><p className="mt-3 text-2xl font-semibold">₹{p.price}</p><button onClick={()=>add(p.id)} className="voice-button mt-5 w-full">Add to cart</button></article>)}</div><p className="mt-6 text-sm text-[var(--text-secondary)]">No products are owned by PlantVerse. Production checkout requires retailer, payment, tax, inventory, and courier integrations.</p></main>}
