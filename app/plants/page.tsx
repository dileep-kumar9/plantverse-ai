"use client";
import { useState } from "react";
import PageIntro from "@/components/shared/PageIntro";
const initial=[{name:"Tomato",local:"టమాటా",place:"Terrace",health:92,icon:"🍅"},{name:"Rose",local:"గులాబీ",place:"Front garden",health:74,icon:"🌹"},{name:"Mango",local:"మామిడి",place:"Backyard",health:88,icon:"🌳"}];
export default function PlantsPage(){const [plants,setPlants]=useState(initial); return <main className="page-wrap"><PageIntro eyebrow="Your garden" title="My Plants" description="A living profile for every plant, including scans, tasks, treatments, and progress." action={<button onClick={()=>setPlants([...plants,{name:"New Plant",local:"Local name",place:"Unassigned",health:100,icon:"🪴"}])} className="voice-button">Add plant</button>}/><div className="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{plants.map((p,i)=><article key={i} className="feature-card"><div className="text-5xl">{p.icon}</div><h2 className="mt-5 text-xl font-semibold">{p.local}</h2><p className="text-sm text-[var(--text-secondary)]">{p.name} · {p.place}</p><div className="mt-5 h-2 rounded-full bg-[var(--surface-secondary)]"><div className="h-full rounded-full bg-[var(--brand-primary)]" style={{width:`${p.health}%`}}/></div><p className="mt-2 text-sm font-semibold text-[var(--brand-primary)]">Health {p.health}%</p></article>)}</div></main>}
